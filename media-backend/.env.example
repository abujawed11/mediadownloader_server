ENV=dev
HOST=0.0.0.0
PORT=8000

REDIS_URL=redis://localhost:6379/0
RQ_QUEUE=media
RQ_JOB_TTL=86400
RQ_RESULT_TTL=604800
RQ_FAILURE_TTL=604800

STORAGE_DIR=./storage
TMP_DIR=./.tmp

# Allow Expo dev clients (adjust as needed)
CORS_ORIGINS=*

# Set for CDN/Nginx URLs if you want absolute links later
# PUBLIC_BASE_URL=http://192.168.1.100:8000/files
